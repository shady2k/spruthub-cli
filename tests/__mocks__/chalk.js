const chalkMethods = {
  green: (str) => str,
  red: (str) => str,
  yellow: (str) => str,
  blue: (str) => str,
  gray: (str) => str,
  bold: (str) => str,
  dim: (str) => str,
  cyan: (str) => str,
  magenta: (str) => str,
  white: (str) => str
};

module.exports = chalkMethods;
module.exports.default = chalkMethods;